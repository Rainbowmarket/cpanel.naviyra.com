# Naviyra — WebSocket Connection header map (http context).
# Installed as: /etc/nginx/conf.d/naviyra-websocket-map.conf
# Ubuntu's nginx.conf includes conf.d/*.conf inside the http {} block.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
