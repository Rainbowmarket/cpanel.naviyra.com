[Unit]
Description=Run Naviyra visitor ingest every minute

[Timer]
# Calendar trigger does not require a previous oneshot run.
# OnUnitActiveSec alone never fires if the service was never started.
OnCalendar=minutely
OnBootSec=45s
Persistent=true
AccuracySec=15s

[Install]
WantedBy=timers.target
