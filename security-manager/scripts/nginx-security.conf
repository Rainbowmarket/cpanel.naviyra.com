# Naviyra Security Manager — Nginx snippet
# Include inside each server {} block:
#   include /etc/nginx/naviyra-security.conf;

# Block list (auto-generated by Security Manager)
include /etc/nginx/naviyra-blocked-ips.conf;

# Optional: pass requests to PHP tracker (real-time ingest)
# location = /naviyra-track {
#     internal;
#     proxy_pass http://127.0.0.1:8090/api/ingest;
#     proxy_set_header X-Ingest-Key YOUR_INGEST_KEY;
# }

# Rate limiting zones (add to http {} once)
# limit_req_zone $binary_remote_addr zone=naviyra_login:10m rate=5r/m;
# limit_req_zone $binary_remote_addr zone=naviyra_general:10m rate=60r/s;

# Brute-force protection on login paths
# location ~* ^/(wp-login|login|admin|xmlrpc) {
#     limit_req zone=naviyra_login burst=10 nodelay;
#     try_files $uri $uri/ =404;
# }
